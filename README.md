# BioDesk React Native App

Offline-first React Native (Expo) scaffold for product detailing, lead capture, and local admin utilities.

## Run

1. `npm install`
2. `npm start`

Default admin PIN (demo only): `1357`

## Note on assets

This repository intentionally avoids committed binary image assets (icon/splash) to keep Codex PR tooling compatible.
